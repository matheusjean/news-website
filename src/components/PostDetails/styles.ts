import styled, { css } from 'styled-components'

export const Container = styled.div`
  ${({ theme }) => css`
    color: #333333;
    font-size: 1rem;
    margin: 2rem 0;
    font-style: italic;
  `}
`
